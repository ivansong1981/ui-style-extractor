interface:
  display_name: "UI Style Extractor"
  short_description: "Extract website UI styles into reusable guides"
  default_prompt: "Use $ui-style-extractor to extract website design systems into markdown style guides and generate validation pages in the same visual language."
